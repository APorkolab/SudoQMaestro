export const environment = {
  production: true,
  apiUrl: '/api',
  oauthClientId: '',
  enableDebugTools: false,
  enableMocking: false,
  logLevel: 'error',
  version: require('../../package.json').version,
};
